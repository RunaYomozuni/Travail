let ez = prompt("Si tu dit oui ou non tu perd, tu es prêt a joué ?");

do {
    let ez = prompt("Si tu dit oui tu perd tu es prêt a joué ?");
    if (ez === "oui" || ez === "non"){
        alert("ez noob ta perdu !!")
    }²
}
while (ez === "oui" || ez === "non");